- config/logs/directories

# cd /etc/docker/
# vi daemon.json
{
  "debug": true
}

- dockerd-Schlüssel für TLS-Verbindungen im Web-Format, den docker verwendet, wenn es sich mit anderen TLS-Servern wie Registraturen verbindet. 
  Er wird von der Docker-Engine beim Start automatisch generiert, so dass kein Risiko besteht, wenn er gelöscht wird
# sudo cat key.json


# systemctl restart docker
# journalctl -u docker

# docker info

# ll /etc/docker/
# ll /etc/containerd/
# ll /etc/containers/

# ll /var/lib/containerd/
# ll /var/lib/docker/
# ll /var/lib/docker-engine/


# ll /var/run/docker*

# ll /etc/sub*id
-rw-r--r-- 1 root root 0 Oct 30 20:48 /etc/subgid
-rw-r--r-- 1 root root 0 Oct 30 20:48 /etc/subuid
- https://docs.docker.com/engine/security/userns-remap/

